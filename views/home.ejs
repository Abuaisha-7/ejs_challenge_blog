<%- include('partials/header'); -%>
<h1>Home</h1>
<p><%= startingContent %></p>

   <% posts.forEach(post => { %>
    <h1><%= post.title %></h1> 
     <P><%= post.content %></P> 
       <% console.log(post.title);%>
    <% }); %>
    
<%- include('partials/footer'); -%>